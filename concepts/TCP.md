# TCP

#개념

TCP는 순서 보장과 재전송을 제공하는 연결 지향 전송 프로토콜이다. HTTP와 TLS가 안정적인 바이트 스트림 위에서 동작하게 만든다.

프론트엔드에서는 직접 소켓을 다루지 않아도 연결 설정 비용과 신뢰성, 초기 지연을 이해할 때 중요하다. 네트워크 성능 문제를 볼 때 기초가 된다.

## 관련 개념
[[TCP 3-way handshake]]
[[TCP 4-way handshake]]
[[TLS]]
