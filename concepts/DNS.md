# DNS

#개념

DNS는 사람이 읽기 쉬운 도메인 이름을 네트워크가 사용할 수 있는 IP 주소로 바꾸는 분산 이름 해석 시스템이다. 웹에서 URL 입력 뒤 실제 서버를 찾기 위한 첫 관문이다.

프론트엔드에서는 직접 만지지 않는 것처럼 보여도 초기 접속 지연과 장애 분석에 중요한 요소다. 성능 문제를 네트워크 단계에서 분리해 볼 때 기본 개념이 된다.

## 관련 개념
[[DNS 리졸버]]
[[권한 DNS 서버]]
[[IP 주소]]
