# 렌더링 엔진

#개념

렌더링 엔진은 HTML, CSS 등 웹 리소스를 해석해 실제 화면으로 그려 내는 브라우저 내부 구성요소다. 파싱, 스타일 계산, 레이아웃, 페인트를 담당하는 중심 축이다.

프론트엔드에서는 브라우저별 동작 차이와 렌더링 성능을 이해할 때 중요하다. 프레임워크가 있더라도 최종 실행 주체는 렌더링 엔진이다.

## 관련 개념
[[웹 브라우저]]
[[렌더 트리]]
