# URL

#개념

URL은 웹 리소스의 위치와 접근 방식을 표현하는 주소 형식이다. 스킴, 호스트, 경로, 쿼리 같은 구성요소를 통해 브라우저가 어디에 무엇을 요청할지 알게 한다.

프론트엔드에서는 라우팅, 링크 설계, 캐시 분리, 상태 표현에 직접 연결된다. URL 구조는 곧 사용자 경험 구조이기도 하다.

## 관련 개념
[[HTTP]]
[[DNS]]
