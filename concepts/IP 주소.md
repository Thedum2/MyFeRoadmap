# IP 주소

#개념

IP 주소는 네트워크에서 호스트를 식별하기 위한 숫자 기반 주소다. 도메인 이름과 달리 라우팅 가능한 실제 목적지를 가리킨다.

프론트엔드에서는 URL 뒤의 실제 네트워크 목적지를 이해할 때 필요하다. 다만 일반적으로는 DNS와 CDN, 프록시 계층 뒤에 감춰져 직접 다루는 일은 적다.

## 관련 개념
[[DNS]]
[[URL]]
