# DNS 리졸버

#개념

DNS 리졸버는 클라이언트 대신 DNS 질의를 수행해 최종 IP 주소를 찾아오는 재귀 질의 서버다. 운영체제나 네트워크 설정을 통해 보통 이 서버를 사용하게 된다.

프론트엔드에서는 직접 구현 대상은 아니지만, DNS 지연과 캐시 효과를 이해할 때 중요하다. 접속 체감 속도는 리졸버 상태에도 영향을 받는다.

## 관련 개념
[[DNS]]
[[권한 DNS 서버]]
