# 마이크로태스크

#개념

마이크로태스크는 현재 실행 중인 태스크가 끝난 직후, 다음 렌더링 전에 우선 처리되는 작은 작업 큐다. `Promise.then` 같은 콜백이 대표적이다.

프론트엔드에서는 비동기처럼 보이더라도 화면 갱신보다 먼저 실행된다는 점이 중요하다. 과도하게 쌓이면 렌더링 기회를 지연시킬 수 있다.

## 관련 개념
[[이벤트 루프]]
[[태스크 큐]]
